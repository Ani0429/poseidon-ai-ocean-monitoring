import "../styles/sections.css";

export default function HealthScore() {
  return (
    <section className="section">
      <h2>Ocean Health Score</h2>
      <p>
        Each ocean region is assigned a score (0–100) representing ecosystem
        health using temperature, pH, oxygen, and chlorophyll indicators.
      </p>
    </section>
  );
}
